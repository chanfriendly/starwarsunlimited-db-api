# ML/AI dependencies — not needed for the core app
# Install with: pip install -r requirements-ml.txt
# Required only when building vector search features (not yet implemented)
qdrant-client==1.13.2
sentence-transformers==3.4.1
numpy==2.2.3
torch==2.6.0
transformers==5.9.0
